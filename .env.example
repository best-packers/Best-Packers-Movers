PORT=3000
NODE_ENV=development
JWT_SECRET=your_jwt_secret_key_here
ADMIN_USERNAME=admin
ADMIN_PASSWORD=admin123

# Database configuration:
# If DATABASE_URL is left empty, the application will fallback to local SQLite (database.sqlite)
# For production (Vercel/Neon/Supabase), set:
# DATABASE_URL=postgresql://username:password@hostname:port/dbname?sslmode=require
DATABASE_URL=
